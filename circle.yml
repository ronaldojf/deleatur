test:
  pre:
    - rake db:drop
    - rake db:create
    - rake db:migrate
  post:
    - bundle exec rake

deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:deleatur.git
      - heroku run rake db:migrate --app deleatur