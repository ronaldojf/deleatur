<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <span>
            <%= image_tag 'deleatur-xs.png', class: 'img-circle', alt: 'Deleatur' %>
          </span>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="clear">
              <span class="block m-t-xs">
                <strong class="font-bold"><%= current_user.name %></strong>
              </span>
              <span class="text-muted text-xs block">
                <%= current_user.email %>
                <b class="caret"></b>
              </span>
            </span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
            <li>
              <%= link_to "#" do %>
                <i class="fa fa-edit"></i> <%= t('profile.edit') %>
              <% end %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to send("destroy_#{current_user_type}_session_path"), method: :delete,
                  data: {confirm: t('js.messages.contents.signout')} do %>
                <i class="fa fa-sign-out"></i> <%= t('profile.signout') %>
              <% end %>
            </li>
          </ul>
        </div>
      </li>
      <%= render partial: "layouts/#{current_user_type}_nav_items" %>
    </ul>
  </div>
</nav>