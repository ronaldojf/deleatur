<%= simple_form_for([:admin, @subject]) do |f| %>

  <div class="form-inputs">
    <div class="row">
      <div class="col-md-12">
        <%= f.input :description, input_html: { 'responsive-focus' => true } %>
      </div>
    </div>

    <%= f.submit '', class: 'hide' %>
  </div>
<% end %>
